# خطوات إضافة لون جديد لبوت الفينيل (مثال حقيقي مطبّق: koi و kiss)

⚠️ **مهم جدًا:** فيه لونين مسار كامل بالبوت لازم تضيف اللون بهم **الاثنين**، وإلا اللون
يشتغل بمكان وما يظهر/ما يشتغل بالثاني:

1. **قائمة `/dev` + قائمة "🎨 لون القرص"** (`build_vinyl_color_keyboard` + `on_vinyl_choice`)
2. **معالج التخصيص "🎛 تخصيص" اللي يطلع بعد إرسال الملف الصوتي** (`build_wiz_color_keyboard` + `on_wiz_color`)

نسيان المسار الثاني (الـ Wizard) هو السبب الأشهر إن اللون "ما يظهر" لما ترسل ملف
وتضغط تخصيص — لأنه كيبورد منفصل تمامًا وفيه تحقق (validation) منفصل من القيم المسموحة.

🩹 **درس من واقعة koi/kiss:** لما أضيفت هذي الألوان أول مرة صار خطأين سكتا بصمت
بدون أي Exception واضح:
- `SHADOW_KOI_PATH` و `SHADOW_KISS_PATH` ما كانوا مضافين بـ `config.py` أصلاً،
  فـ `get_developer_shadow_path` كانت ترجع الظل الأسود الافتراضي دايمًا (بدون خطأ،
  لأنها ببساطة توصل لآخر شرط `return config.SHADOW_PATH` العادي).
- بدالة `get_developer_vinyl_path` كان فيه خطأ إملائي: `if choics == "kiss":`
  بدل `choice` — والنتيجة كانت اختيار "kiss" يرجع صامتًا للقرص الأسود الافتراضي
  بدل ما يطيح بخطأ واضح.

**الخلاصة:** بعد أي إضافة، افتح الملف واعمل `grep` على اسم اللون الجديد بالكامل
(`grep -n "koi\|kiss" config.py handlers.py texts.py`) وتأكد كل نتيجة منطقية —
هذا أسرع من تشغيل البوت واختباره يدويًا لاكتشاف "رجع أسود بدل ما توقعت".

---

## 1) تجهيز ملفات القالب (assets/)
جهّز ملفين PNG بنفس مقاس 640×640 وبنفس تسمية الألوان الموجودة:

```
assets/vinyl_koi.png     # صورة القرص
assets/shadow_koi.png    # طبقة الظل/الإضاءة المطابقة له
assets/vinyl_kiss.png
assets/shadow_kiss.png
```

⚠️ **لا تنسَ ملف الـ shadow.** هذا هو الخطأ الأكثر شيوعًا: القرص يبني نفسه صح،
لكن الفيديو يطلع بظل القرص الأسود الافتراضي لأن `SHADOW_<NAME>_PATH` ناقصة
بـ config.py (انظر القسم 2).

---

## 2) config.py — إضافة مسارات الملفات

أضف زوج المسارين **معًا** بجانب `VINYL_PINK_PATH` / `SHADOW_PINK_PATH` وأخواتها:

```python
VINYL_KOI_PATH = os.path.join(ASSETS_DIR, "vinyl_koi.png")
SHADOW_KOI_PATH = os.path.join(ASSETS_DIR, "shadow_koi.png")
VINYL_KISS_PATH = os.path.join(ASSETS_DIR, "vinyl_kiss.png")
SHADOW_KISS_PATH = os.path.join(ASSETS_DIR, "shadow_kiss.png")
```

---

## 3) texts.py — إضافة نص الزر (عربي + إنكليزي)

أضف بجانب `BTN_VINYL_PINK` وأخواتها (بالقسم العربي):

```python
BTN_VINYL_KOI = "🎏 KOI"
BTN_VINYL_KISS = "💋 KISS"
```

وبقاموس `TEXTS_EN`:

```python
"BTN_VINYL_KOI": "🎏 KOI",
"BTN_VINYL_KISS": "💋 KISS",
```

---

## 4) handlers.py — المسار الأول: /dev وقائمة لون القرص العادية

### أ) get_developer_vinyl_path — أضف شرط جديد

```python
def get_developer_vinyl_path(user_id: int, choice_override: str | None = None) -> str:
    choice = choice_override if choice_override is not None else developer_vinyl_choice.get(user_id)
    ...
    if choice == "koi":
        return config.VINYL_KOI_PATH
    if choice == "kiss":           # ⚠️ تأكد الاسم "choice" مو خطأ إملائي زي "choics"
        return config.VINYL_KISS_PATH
    return config.VINYL_PATH
```

### ب) get_developer_shadow_path — نفس الشي بالضبط

```python
def get_developer_shadow_path(user_id: int, choice_override: str | None = None) -> str:
    choice = choice_override if choice_override is not None else developer_vinyl_choice.get(user_id)
    ...
    if choice == "koi":
        return config.SHADOW_KOI_PATH
    if choice == "kiss":
        return config.SHADOW_KISS_PATH
    return config.SHADOW_PATH
```

### ج) on_vinyl_choice — أضف القيم الجديدة لقائمة الألوان المقبولة

⚠️ هذا أهم سطر ينسى — لو اللون مو موجود بهذي القائمة، اختيار المستخدم يُتجاهل
ويرجع تلقائيًا للون الافتراضي (default) حتى لو الزر نفسه ظاهر بالكيبورد.

```python
@router.callback_query(F.data.startswith("vinyl:"))
async def on_vinyl_choice(callback, bot: Bot):
    choice = callback.data.split(":", 1)[1]
    user_id = callback.from_user.id if callback.from_user else 0
    if choice in ("pink", "blue", "yellow", "red", "green", "bloody",
                  "rose", "emerald", "koi", "kiss"):          # 👈 أضف هنا
        developer_vinyl_choice[user_id] = choice
    else:
        developer_vinyl_choice.pop(user_id, None)
    await callback.message.edit_reply_markup(reply_markup=build_vinyl_color_keyboard(user_id))
    await callback.answer(tr("MSG_VINYL_CHOICE_SAVED_ANSWER", user_id))
```

### د) build_vinyl_color_keyboard — أضف الزر الجديد بالكيبورد

```python
[
    InlineKeyboardButton(
        text=label("BTN_VINYL_KOI", "koi"),
        callback_data="vinyl:koi",
        style=btn_style("koi")
    ),
    InlineKeyboardButton(
        text=label("BTN_VINYL_KISS", "kiss"),
        callback_data="vinyl:kiss",
        style=btn_style("kiss")
    ),
],
```

---

## 5) handlers.py — المسار الثاني (المنسي): معالج التخصيص "🎛 تخصيص"

هذا المسار يظهر لما المستخدم يرسل ملف صوتي ويضغط "🎛 تخصيص" (`mode:custom`).
له كيبورد وتحقق منفصلين تمامًا عن المسار الأول، ولازم تعدّلهم بشكل مستقل.

### أ) build_wiz_color_keyboard — أضف الزر الجديد

```python
[
    InlineKeyboardButton(
        text=tr("BTN_VINYL_KOI", user_id),
        callback_data=cb("wiz_color:koi"),
        style="primary"
    ),
    InlineKeyboardButton(
        text=tr("BTN_VINYL_KISS", user_id),
        callback_data=cb("wiz_color:kiss"),
        style="primary"
    ),
],
```

### ب) on_wiz_color — أضف القيم الجديدة لقائمة القيم المقبولة

⚠️ **هذا هو السطر اللي غالبًا كان ناقص عندك.** بدونه، اختيار اللون الجديد بالـ
wizard يُتجاهل ويرجع تلقائيًا للأسود الافتراضي:

```python
@router.callback_query(F.data.startswith("wiz_color:"))
async def on_wiz_color(callback, bot: Bot):
    ...
    choice = base.split(":", 1)[1]
    if choice in ("green", "pink", "blue", "yellow", "red", "bloody",
                  "rose", "emerald", "koi", "kiss"):           # 👈 أضف هنا
        developer_vinyl_choice[uid] = choice
    else:
        developer_vinyl_choice.pop(uid, None)
    ...  # ينتقل مباشرة لخطوة اختيار السرعة (wiz_speed)
```

---

## 6) اختبار شامل (المسارين الاثنين)

**المسار الأول (/dev أو زر "🎨 لون القرص" من /start):**
- شغّل البوت، اضغط /start، اضغط زر "🎨 لون القرص" (`vinyl_menu:open`)
- تأكد اللون الجديد يظهر بالكيبورد
- اختره، تأكد يظهر ✅ جنبه بدون أي كراش

**المسار الثاني (معالج التخصيص — المكان اللي غالبًا كانت فيه المشكلة):**
- أرسل ملف صوتي للبوت
- اضغط "🎛 تخصيص" (مو "⚡ إنشاء سريع")
- بشاشة اختيار اللون، تأكد اللون الجديد ظاهر بالزر
- اضغطه وكمّل الخطوات (السرعة → الصورة → الإرسال)
- تأكد الفيديو الناتج فعلاً يستخدم `vinyl_koi.png` و `shadow_koi.png` (أو kiss)
  — **مو الأسود الافتراضي.** لو رجع أسود، دقق الملاحظات بالمقدمة (شرط الـ
  shadow المفقود أو خطأ إملائي بمقارنة القيمة).

**نصيحة عامة:** أي لون جديد لازم يتكرر بست أماكن بالضبط بملف handlers.py:
`get_developer_vinyl_path`, `get_developer_shadow_path`, `on_vinyl_choice`,
`build_vinyl_color_keyboard`, `build_wiz_color_keyboard`, `on_wiz_color` —
بالإضافة لزوج المسارين بـ config.py (VINYL + SHADOW) وزوج النصوص بـ texts.py
(عربي + TEXTS_EN). فوّت أي وحدة = اللون يشتغل جزئيًا بس ويسبب لغبطة "ليش ما
يشتغل؟".

---

## ملاحظة: نظام "الإطار الفضي" أُزيل بالكامل

كان فيه سابقًا خطوة اختيارية بمعالج التخصيص لإضافة "إطار فضي" حول القرص
(`build_wiz_frame_keyboard`, `on_wiz_frame`, `developer_frame_choice`,
`FRAME_SILVER_PATH`, `FRAME_SILVER_DISC_RATIO`, `resize_disc_for_frame`،
ومعامل `frame_path` بدالة `render_vinyl`). تم حذف هذا النظام بالكامل من
الكود (handlers.py, config.py, compose.py, processor.py, texts.py)، وتدفق
الـ Wizard الآن ينتقل مباشرة من خطوة اختيار اللون إلى خطوة اختيار السرعة
بدون أي خطوة إطار بالمنتصف.
